server:
    port: 8081

spring:
  cloud:
    config:
      server:
        git:
          default-label: eureka
          uri: https://github.com/NicolasPerezU/microservice-video3
          search-paths: config-data

  application:
    name: config-server